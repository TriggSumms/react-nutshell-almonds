//import React, {} from "react";
// friendManager
//MessageCard

/* const userId=activeUserId;

const MessageToFriend = (props) => {
const [friend, setFriend] = useState({activeUserId:""});

//const [message, setMessage] = seState({});


useEffect(() => {
  //got here now make call to get employee with animal
  Manager.FriendWithMessage(props.match.params.friend.activeUserId)
  .then(APIResult);
  setFriend(APIResult);
});
}, []);

return (

<>
</>

)

} */

//export default MessageToFriend

